# Python deps for the web server (slice rendering, crowding/GVI API).
# Install from repo root:
#   python3 -m venv .venv
#   .venv/bin/pip install -r frontend/requirements.txt
#   .venv/bin/pip install -e downstream[gnn]

numpy>=1.24
scipy>=1.10
zarr>=2.16
pandas>=2.0
tqdm>=4.66
torch>=2.0
torch-geometric>=2.4
scikit-learn>=1.3
